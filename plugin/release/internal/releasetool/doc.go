// Package releasetool owns the static identities and V1 behavior facts shared
// by Release Plugin configuration, validation, planning, and compatibility
// adapters. It deliberately provides facts rather than runtime registration or
// a generic release-tool execution abstraction.
package releasetool
