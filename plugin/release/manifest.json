{
  "name": "release",
  "version": "4.2.0",
  "description": "Release management plugin",
  "author": "nekoman-hq",
  "commands": [
    {
      "name": "init",
      "description": "Initialize release system with project configuration",
      "outputs": [
        "text",
        "json"
      ],
      "flags": [
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "default": "cli",
          "description": "Release unit id"
        },
        {
          "name": "display-name",
          "type": "string",
          "required": false,
          "description": "Release unit display name"
        },
        {
          "name": "version",
          "type": "string",
          "required": false,
          "default": "0.1.0",
          "description": "Initial version (semver)"
        },
        {
          "name": "executor",
          "type": "string",
          "required": true,
          "description": "Release executor (goreleaser|jreleaser|release-it)"
        },
        {
          "name": "delivery",
          "type": "string",
          "required": true,
          "description": "Release delivery mode (github-actions)"
        },
        {
          "name": "workflow",
          "type": "string",
          "required": false,
          "description": "GitHub Actions workflow path, required for V2 github-actions delivery"
        },
        {
          "name": "tag-prefix",
          "type": "string",
          "required": false,
          "default": "v",
          "description": "Release tag prefix"
        },
        {
          "name": "working-directory",
          "type": "string",
          "required": false,
          "default": ".",
          "description": "Unit working directory"
        },
        {
          "name": "paths",
          "type": "string",
          "required": false,
          "default": "**",
          "description": "Comma-separated unit path globs"
        },
        {
          "name": "kind",
          "type": "string",
          "required": false,
          "default": "release",
          "description": "Release unit kind: release (default) for normal services/apps/libraries/SDKs/CLIs without plugin metadata; plugin only for Neko CLI plugins and requires plugin metadata flags"
        },
        {
          "name": "plugin-name",
          "type": "string",
          "required": false,
          "description": "Public Neko CLI plugin name; only with --kind plugin and required there"
        },
        {
          "name": "plugin-manifest",
          "type": "string",
          "required": false,
          "description": "Repository-root-relative Neko CLI plugin manifest path; only with --kind plugin and required there"
        },
        {
          "name": "plugin-asset-prefix",
          "type": "string",
          "required": false,
          "description": "Neko CLI plugin release asset prefix; only with --kind plugin, required there, and must equal unit id"
        },
        {
          "name": "plugin-binary-name",
          "type": "string",
          "required": false,
          "description": "Neko CLI plugin executable name in release archives; only with --kind plugin and required there"
        },
        {
          "name": "force",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Overwrite existing V2 config/state"
        }
      ]
    },
    {
      "name": "unit-add",
      "description": "Add a release unit to an existing V2 release configuration",
      "outputs": [
        "table",
        "json",
        "text"
      ],
      "flags": [
        {
          "name": "unit",
          "type": "string",
          "required": true,
          "description": "Release unit id to append"
        },
        {
          "name": "display-name",
          "type": "string",
          "required": false,
          "description": "Release unit display name"
        },
        {
          "name": "version",
          "type": "string",
          "required": false,
          "default": "0.1.0",
          "description": "Initial version (semver)"
        },
        {
          "name": "executor",
          "type": "string",
          "required": true,
          "description": "Release executor (goreleaser|jreleaser|release-it)"
        },
        {
          "name": "delivery",
          "type": "string",
          "required": true,
          "description": "Release delivery mode (github-actions)"
        },
        {
          "name": "workflow",
          "type": "string",
          "required": false,
          "description": "GitHub Actions workflow path, required for V2 github-actions delivery"
        },
        {
          "name": "tag-prefix",
          "type": "string",
          "required": false,
          "default": "v",
          "description": "Release tag prefix"
        },
        {
          "name": "working-directory",
          "type": "string",
          "required": false,
          "default": ".",
          "description": "Unit working directory"
        },
        {
          "name": "paths",
          "type": "string",
          "required": false,
          "default": "**",
          "description": "Comma-separated unit path globs"
        },
        {
          "name": "kind",
          "type": "string",
          "required": false,
          "default": "release",
          "description": "Release unit kind: release (default) for normal services/apps/libraries/SDKs/CLIs without plugin metadata; plugin only for Neko CLI plugins and requires plugin metadata flags"
        },
        {
          "name": "plugin-name",
          "type": "string",
          "required": false,
          "description": "Public Neko CLI plugin name; only with --kind plugin and required there"
        },
        {
          "name": "plugin-manifest",
          "type": "string",
          "required": false,
          "description": "Repository-root-relative Neko CLI plugin manifest path; only with --kind plugin and required there"
        },
        {
          "name": "plugin-asset-prefix",
          "type": "string",
          "required": false,
          "description": "Neko CLI plugin release asset prefix; only with --kind plugin, required there, and must equal unit id"
        },
        {
          "name": "plugin-binary-name",
          "type": "string",
          "required": false,
          "description": "Neko CLI plugin executable name in release archives; only with --kind plugin and required there"
        }
      ]
    },
    {
      "name": "init-options",
      "description": "Get available options for init command",
      "outputs": [
        "json"
      ]
    },
    {
      "name": "migrate",
      "description": "Safely migrate a root V1 release configuration to V2",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "dry-run",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Preview the migration without writing files"
        }
      ]
    },
    {
      "name": "patch",
      "description": "Create a patch release (x.y.Z)",
      "outputs": [
        "text",
        "json"
      ],
      "flags": [
        {
          "name": "dry-run",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Run without making changes"
        },
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Selects the release unit. Required when a V2 repository defines multiple units."
        }
      ]
    },
    {
      "name": "minor",
      "description": "Create a minor release (x.Y.0)",
      "outputs": [
        "text",
        "json"
      ],
      "flags": [
        {
          "name": "dry-run",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Run without making changes"
        },
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Selects the release unit. Required when a V2 repository defines multiple units."
        }
      ]
    },
    {
      "name": "major",
      "description": "Create a major release (X.0.0)",
      "outputs": [
        "text",
        "json"
      ],
      "flags": [
        {
          "name": "dry-run",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Run without making changes"
        },
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Selects the release unit. Required when a V2 repository defines multiple units."
        }
      ]
    },
    {
      "name": "plan",
      "description": "Inspect the local release plan without reading tokens, journals, remotes, or starting release execution",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "change",
          "type": "string",
          "required": true,
          "description": "Requested version change to inspect: patch, minor, or major"
        },
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Selects the release unit. Required when a V2 repository defines multiple units."
        }
      ]
    },
    {
      "name": "doctor",
      "description": "Diagnose Release V2 GitHub Actions integration readiness locally, with optional explicit read-only remote verification",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Inspect one Release V2 unit while retaining checks for every unit sharing its workflow"
        },
        {
          "name": "verify-remote",
          "type": "bool",
          "required": false,
          "description": "Explicitly verify supported GitHub metadata with GET-only read requests"
        }
      ]
    },
    {
      "name": "units",
      "description": "List configured Release V2 units, current state versions, tag shapes, metadata, and alignment issues without workflow, Git, network, or token inspection",
      "outputs": [
        "table",
        "json"
      ],
      "flags": []
    },
    {
      "name": "pipeline",
      "description": "Inspect one Release V2 pipeline; default human output is concise and includes actionable findings, global --describe adds all structured inspection details, global --verbose adds execution logs, and --verify-remote explicitly enables bounded GET-only remote verification",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Selects the release unit. Required when a V2 repository defines multiple units."
        },
        {
          "name": "verify-remote",
          "type": "bool",
          "required": false,
          "description": "Explicitly enables bounded GitHub GET-only verification and optional read-token resolution; the default remains local, offline, and token-free."
        }
      ]
    },
    {
      "name": "ci-validate-context",
      "description": "Validate a dispatched Release V2 context against local config, state, HEAD, and tag history without mutation or network access",
      "outputs": [
        "table",
        "json",
        "github"
      ],
      "flags": [
        {
          "name": "unit",
          "type": "string",
          "required": true,
          "description": "Dispatched release unit id"
        },
        {
          "name": "version",
          "type": "string",
          "required": true,
          "description": "Dispatched canonical semantic version"
        },
        {
          "name": "tag",
          "type": "string",
          "required": true,
          "description": "Dispatched canonical release tag"
        },
        {
          "name": "release-sha",
          "type": "string",
          "required": true,
          "description": "Dispatched full lowercase release commit object id"
        }
      ]
    },
    {
      "name": "github-workflow-init",
      "description": "Create or preview the configured canonical GitHub Actions release workflow without overwriting different content",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Select a Release V2 unit and its configured workflow path"
        },
        {
          "name": "path",
          "type": "string",
          "required": false,
          "description": "Select an exact repository-root-relative workflow path already configured by a Release V2 unit"
        },
        {
          "name": "dry-run",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Preview exact generated workflow content without writing files"
        }
      ]
    },
    {
      "name": "resume",
      "description": "Resumes a previously journaled V2 GitHub Actions release. It never calculates a new version and does not automatically retry uncertain push or dispatch outcomes.",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Selects the release unit whose existing V2 GitHub Actions journal should be resumed."
        },
        {
          "name": "dry-run",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Assess the existing release journal without writing files, refs, journals, remotes, or dispatching."
        }
      ]
    },
    {
      "name": "history",
      "description": "Show release history",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Selects the release unit. Required when a V2 repository defines multiple units."
        }
      ]
    },
    {
      "name": "contributors",
      "description": "Show repository contributors",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Selects the release unit. Required when a V2 repository defines multiple units."
        }
      ]
    },
    {
      "name": "validate",
      "description": "Validate the complete release configuration",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "show",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Display structured release configuration details and unit summaries"
        },
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Focus displayed V2 unit details; the complete repository is still validated."
        }
      ]
    },
    {
      "name": "evidence",
      "description": "Inspect release evidence journals without mutating recovery state",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "family",
          "type": "string",
          "required": false,
          "description": "Filter evidence by family: release-execution, dispatch, migration, v1-compensation, or v2-pair-recovery"
        },
        {
          "name": "unit",
          "type": "string",
          "required": false,
          "description": "Filter evidence records by release unit when the evidence family records a unit"
        },
        {
          "name": "identity",
          "type": "string",
          "required": false,
          "description": "Inspect one complete Evidence record by an unambiguous 8-64 character lowercase hexadecimal identity prefix after family and unit filters"
        }
      ]
    },
    {
      "name": "evidence-archive",
      "description": "Archive one completed release evidence file after explicit confirmation",
      "outputs": [
        "table",
        "json"
      ],
      "flags": [
        {
          "name": "family",
          "type": "string",
          "required": true,
          "description": "Evidence family to archive; only completed release-execution, v1-compensation, and v2-pair-recovery evidence are supported"
        },
        {
          "name": "identity",
          "type": "string",
          "required": true,
          "description": "Evidence identity from inspection output"
        },
        {
          "name": "digest-sha256",
          "type": "string",
          "required": true,
          "description": "Current evidence digest from inspection output; used to reject stale archive requests"
        },
        {
          "name": "confirm-archive",
          "type": "bool",
          "required": true,
          "default": false,
          "description": "Required explicit confirmation for archive-completed"
        }
      ]
    },
    {
      "name": "plugin-index",
      "description": "Generate the public plugin registry index from V2 plugin units",
      "outputs": [
        "json",
        "table"
      ],
      "flags": [
        {
          "name": "output",
          "type": "string",
          "required": false,
          "description": "Optional file path to write plugin-index.json"
        },
        {
          "name": "check",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Validate that the plugin index can be generated without writing a file"
        },
        {
          "name": "pretty",
          "type": "bool",
          "required": false,
          "default": true,
          "description": "Pretty-print generated JSON"
        },
        {
          "name": "repository",
          "type": "string",
          "required": false,
          "default": "nekoman-hq/neko-cli",
          "description": "Repository identifier to include in the generated index"
        }
      ]
    }
  ],
  "renderer_types": [
    "table",
    "json",
    "github",
    "text"
  ]
}
